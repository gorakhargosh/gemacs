# -*- mode: snippet -*-
# name: for (Iterator ...) { ... }
# key: forit
# expand-env: ((yas/indent-line 'fixed))
# --
for (Iterator ${1:it} = $2.iterator(); $1.hasNext(); ) {
    ${3:String} ${4:name} = ($3)$1.next();
    $0
}