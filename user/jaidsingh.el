(progn
  (setq user-full-name "Jaideep Singh"
        user-mail-address "jaidsingh@google.com")
  (when window-system
    ;; (require 'monokai-theme)
    ;; (require 'github-theme)
    ;; (require 'tango-2-theme)
    (require 'burp-theme)
    )
  )
(provide 'jaidsingh)
