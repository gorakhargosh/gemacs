(progn
  (setq user-full-name "Udaykanth Rapeti"
        user-mail-address "udaykanth@google.com")
  (when window-system
    (require 'burp-theme)
    ;; (require 'cloud-theme)
    ;; (require 'google-code-theme)
    )
  )
(provide 'udaykanth)
