(progn
  (setq user-full-name "Mohammad Rafi"
        user-mail-address "mrafi@google.com")
  (when window-system
    ;; (require 'monokai-theme)
    ;; (require 'github-theme)
    ;; (require 'tango-2-theme)
    (require 'burp-theme)
  ))
(provide 'mrafi)
