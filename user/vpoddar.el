(progn
  (setq user-full-name "Vivek Poddar"
        user-mail-address "vpoddar@google.com")
  (when window-system
    (require 'burp-theme)
    ;; (require 'cloud-theme)
    ;; (require 'google-code-theme)
    )
  )
(provide 'vpoddar)
