(progn
  (setq user-full-name "Akshay Valsa"
        user-mail-address "avalsa@google.com")
  (when window-system
    (require 'burp-theme)
    ;; (require 'cloud-theme)
    ;; (require 'google-code-theme)
    )
  )
(provide 'avalsa)
