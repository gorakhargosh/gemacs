(progn
  (setq user-full-name "Sameer Mahabole"
        user-mail-address "smahabole@google.com")
  (when window-system
    (require 'burp-theme)
    ;; (require 'cloud-theme)
    ;; (require 'google-code-theme)
    )
  )
(provide 'smahabole)
