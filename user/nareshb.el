(progn
  (setq user-full-name "Naresh Boddepalli"
        user-mail-address "nareshb@google.com")
  (when window-system
    (require 'burp-theme)
    ;; (require 'cloud-theme)
    ;; (require 'google-code-theme)
    )
  )
(provide 'nareshb)
