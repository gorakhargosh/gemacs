(progn
  (setq user-full-name "Taranjeet Singh"
        user-mail-address "taranjeet@google.com")
  (when window-system
    (require 'burp-theme)
    ;; (require 'cloud-theme)
    ;; (require 'google-code-theme)
    )
  )
(provide 'taranjeet)
