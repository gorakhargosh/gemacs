(progn
  (setq user-full-name "Srinath Sarma"
        user-mail-address "srinathsarma@google.com")
  (when window-system
    (require 'burp-theme)
    ;; (require 'cloud-theme)
    ;; (require 'google-code-theme)
    )
  )
(provide 'srinathsarma)
